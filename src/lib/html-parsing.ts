export const extractLoopsVideoSrc = (html: string) => {
  return undefined as string | undefined;
};

export const parseOgData = (html: string) => {
  const title = undefined as undefined | string;
  const image = undefined as undefined | string;
  return {
    title,
    image,
  };
};
